# 빠른 시작 가이드 (Quick Start Guide)

## 1분 만에 시작하기 ⚡

### Step 1: 라이브러리 설치

```bash
pip install openpyxl
```

### Step 2: 프로그램 실행

#### 방법 A: GUI 실행 (추천)

**Windows:**
- `run_gui.bat` 더블클릭

**또는 명령줄:**
```bash
python gui.py
```

#### 방법 B: CLI 실행

```bash
# 디렉토리 파싱
python cli.py -d ./emails

# 파일 파싱
python cli.py -f email.eml
```

### Step 3: 파일 선택 및 저장

1. GUI에서 "파일 선택" 또는 "디렉토리 선택" 클릭
2. .eml 파일 선택
3. "파싱 시작" 버튼 클릭
4. 저장할 엑셀 파일 이름 입력
5. 완료!

## 주요 사용 시나리오

### 시나리오 1: 고객 지원 이메일 정리

```bash
python cli.py -d "C:\고객지원이메일" -o "고객QA.xlsx"
```

이메일 스레드를 자동으로 분석하여 질문과 답변을 깔끔하게 정리합니다.

### 시나리오 2: 프로젝트 이메일 아카이브

```bash
python cli.py -d "C:\프로젝트\이메일" --raw -o "이메일아카이브.xlsx"
```

모든 이메일을 시간순으로 정리하여 아카이브합니다.

### 시나리오 3: 특정 이메일만 선택

GUI를 실행하고 필요한 이메일 파일만 선택하여 처리합니다.

## 파일 구조

```
프로젝트/
├── email_parser.py          # 핵심 파싱 엔진
├── excel_exporter.py        # 엑셀 생성기
├── cli.py                   # 명령줄 도구
├── gui.py                   # 그래픽 인터페이스
├── run_gui.bat              # GUI 실행 스크립트 (Windows)
├── example_usage.py         # 사용 예시
├── EMAIL_PARSER_README.md   # 상세 문서
└── QUICK_START.md          # 이 파일
```

## 옵션 설명

### GUI 옵션

- **질의응답 형식**: 스레드를 분석하여 Q&A 쌍으로 정리
- **원본 이메일 형식**: 이메일을 그대로 나열
- **하위 디렉토리 포함**: 폴더 안의 모든 하위 폴더도 검색

### CLI 옵션

```bash
python cli.py [옵션]

-f, --files FILE [FILE ...]    # 파싱할 파일 지정
-d, --directory DIR             # 파싱할 디렉토리 지정
-o, --output FILE               # 출력 파일명 지정
--raw                          # 원본 형식으로 저장
--no-recursive                 # 하위 디렉토리 검색 안함
```

## 출력 예시

### 질의응답 형식 (기본)

| 번호 | 스레드 제목 | 질문 발신자 | 질문 일시 | 질문 내용 | 답변 발신자 | 답변 일시 | 답변 내용 |
|:----:|-------------|-------------|-----------|-----------|-------------|-----------|-----------|
| 1 | 제품 문의 | customer@example.com | 2025-01-15 10:30 | 배송은 언제... | support@company.com | 2025-01-15 14:20 | 배송은 2-3일... |

### 원본 이메일 형식

| 번호 | 제목 | 발신자 | 수신자 | 일시 | 본문 |
|:----:|------|--------|--------|------|------|
| 1 | 제품 문의 | customer@example.com | support@company.com | 2025-01-15 10:30 | 배송은 언제... |

## 팁과 요령 💡

1. **대량 파일 처리**: 수백 개의 이메일도 몇 초 만에 처리 가능
2. **스레드 자동 인식**: Re:, Fwd: 등을 자동으로 인식하여 스레드 분류
3. **에러 처리**: 일부 파일이 손상되어도 나머지 파일은 계속 처리
4. **멀티파트 지원**: 텍스트와 HTML 본문 모두 자동 처리
5. **예쁜 출력**: 엑셀 파일이 자동으로 포맷팅되어 바로 사용 가능

## 문제 해결

### 오류: ModuleNotFoundError

```bash
pip install openpyxl
```

### GUI가 안 열림

tkinter가 설치되어 있는지 확인:
- Windows: 기본 설치됨
- Linux: `sudo apt-get install python3-tk`

### 한글이 깨짐

이메일 파일의 인코딩을 확인하세요. 프로그램은 UTF-8을 권장합니다.

## 더 자세한 정보

상세한 정보는 `EMAIL_PARSER_README.md`를 참고하세요.

## 지원

문제가 있으면 이슈를 등록해주세요!

---

**즐거운 이메일 파싱 되세요! 🚀**
