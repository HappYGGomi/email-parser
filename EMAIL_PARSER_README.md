# 📧 이메일 파서 (Email Parser)

.eml 파일을 파싱하여 이메일 스레드의 질의응답을 추출하고 엑셀 파일로 저장하는 파이썬 프로그램입니다.

## 주요 기능

- ✅ .eml 파일 파싱 (단일 파일 또는 디렉토리)
- ✅ 이메일 스레드 자동 분류
- ✅ 질의응답(Q&A) 자동 추출
- ✅ 엑셀 파일로 내보내기 (포맷팅 포함)
- ✅ GUI 인터페이스 (tkinter)
- ✅ CLI 인터페이스 (명령줄)
- ✅ 원본 이메일 형식 저장 옵션

## 설치 방법

### 1. 필요 라이브러리 설치

```bash
pip install openpyxl
```

또는 requirements.txt에서 설치:

```bash
pip install -r requirements.txt
```

### 2. Python 버전

Python 3.7 이상이 필요합니다.

## 사용 방법

### GUI 모드 (추천)

그래픽 인터페이스로 쉽게 사용할 수 있습니다.

```bash
python gui.py
```

**GUI 사용 단계:**
1. "파일 선택" 또는 "디렉토리 선택" 버튼 클릭
2. 옵션 설정 (질의응답 형식 또는 원본 이메일 형식)
3. "파싱 시작" 버튼 클릭
4. 저장할 엑셀 파일 이름 입력
5. 파싱 완료!

### CLI 모드

명령줄에서 스크립트로 실행할 수 있습니다.

#### 기본 사용법

```bash
# 단일 파일 파싱
python cli.py -f email1.eml

# 여러 파일 파싱
python cli.py -f email1.eml email2.eml email3.eml

# 디렉토리 파싱 (하위 디렉토리 포함)
python cli.py -d ./emails

# 디렉토리 파싱 (현재 디렉토리만)
python cli.py -d ./emails --no-recursive

# 출력 파일명 지정
python cli.py -d ./emails -o output.xlsx

# 원본 이메일 형식으로 저장
python cli.py -d ./emails --raw
```

#### CLI 옵션

| 옵션 | 설명 |
|------|------|
| `-f, --files` | 파싱할 .eml 파일 경로 (여러 개 지정 가능) |
| `-d, --directory` | 파싱할 디렉토리 경로 |
| `-o, --output` | 출력 엑셀 파일명 (기본값: email_qa_YYYYMMDD_HHMMSS.xlsx) |
| `--raw` | 원본 이메일 형식으로 저장 (질의응답 추출 안함) |
| `--no-recursive` | 하위 디렉토리 검색 안함 |

## 파일 구조

```
.
├── email_parser.py      # 이메일 파싱 코어 모듈
├── excel_exporter.py    # 엑셀 내보내기 모듈
├── cli.py               # CLI 인터페이스
├── gui.py               # GUI 인터페이스
├── requirements.txt     # 필요 라이브러리
└── EMAIL_PARSER_README.md  # 이 파일
```

## 모듈 설명

### 1. email_parser.py

이메일 파싱 및 스레드 분석 핵심 모듈

**주요 클래스:**
- `EmailMessage`: 개별 이메일 메시지 파싱
- `EmailThread`: 이메일 스레드 관리
- `EmailParser`: 전체 파싱 프로세스 관리

**기능:**
- .eml 파일 읽기 및 파싱
- 이메일 제목, 발신자, 수신자, 날짜, 본문 추출
- 멀티파트 메시지 처리
- HTML 본문 처리 (태그 제거)
- 이메일 스레드 자동 분류
- 질의응답 쌍 추출

### 2. excel_exporter.py

엑셀 파일 생성 모듈

**주요 클래스:**
- `ExcelExporter`: 엑셀 파일 생성 및 스타일링

**기능:**
- 질의응답 형식으로 엑셀 저장
- 원본 이메일 형식으로 엑셀 저장
- 자동 컬럼 너비 조정
- 헤더 스타일링 (색상, 폰트)
- 셀 테두리 및 정렬
- 첫 행 고정 (freeze panes)

### 3. cli.py

명령줄 인터페이스

**기능:**
- 파일/디렉토리 파싱
- 다양한 명령줄 옵션 지원
- 진행 상황 표시
- 에러 처리

### 4. gui.py

그래픽 사용자 인터페이스

**기능:**
- 파일 선택 다이얼로그
- 디렉토리 선택 다이얼로그
- 옵션 설정 UI
- 실시간 로그 표시
- 프로그레스 바
- 멀티스레딩 (UI 응답성 유지)

## 출력 형식

### 질의응답 형식 (기본)

엑셀 파일에 다음 컬럼으로 저장됩니다:

| 번호 | 스레드 제목 | 질문 발신자 | 질문 일시 | 질문 내용 | 답변 발신자 | 답변 일시 | 답변 내용 |
|------|-------------|-------------|-----------|-----------|-------------|-----------|-----------|

- 이메일 스레드를 자동으로 분석하여 질문과 답변을 매칭
- 첫 번째 메시지를 질문으로, 두 번째 메시지를 답변으로 처리
- 이후 홀수 번째 메시지는 추가 질문, 짝수 번째는 답변으로 처리

### 원본 이메일 형식

엑셀 파일에 다음 컬럼으로 저장됩니다:

| 번호 | 제목 | 발신자 | 수신자 | 일시 | 본문 |
|------|------|--------|--------|------|------|

- 파싱된 모든 이메일을 날짜순으로 나열
- 질의응답 매칭 없이 원본 그대로 저장

## 사용 예시

### 예시 1: 고객 지원 이메일 분석

```bash
python cli.py -d "C:\Support\Emails" -o "customer_support_qa.xlsx"
```

고객 지원 이메일 디렉토리를 파싱하여 모든 질문과 답변을 엑셀로 정리

### 예시 2: 특정 프로젝트 이메일만 파싱

```bash
python cli.py -f project1.eml project2.eml project3.eml -o "project_emails.xlsx"
```

특정 프로젝트 관련 이메일 파일만 선택하여 파싱

### 예시 3: GUI로 쉽게 사용

```bash
python gui.py
```

GUI를 실행하고 마우스 클릭으로 파일 선택 및 파싱

## 기술 스택

- **Python 3.7+**: 메인 프로그래밍 언어
- **email**: 이메일 파싱 (Python 표준 라이브러리)
- **openpyxl**: 엑셀 파일 생성 및 스타일링
- **tkinter**: GUI 인터페이스 (Python 표준 라이브러리)
- **argparse**: CLI 인터페이스 (Python 표준 라이브러리)

## 특징

### 강점

✅ **사용하기 쉬움**: GUI와 CLI 모두 지원
✅ **빠른 처리**: 대량의 이메일 파일도 빠르게 처리
✅ **자동 스레드 분류**: Re:, Fwd: 등을 자동으로 인식
✅ **멀티파트 지원**: 텍스트와 HTML 본문 모두 처리
✅ **예쁜 엑셀 출력**: 컬럼 너비, 색상, 정렬 자동 처리
✅ **에러 처리**: 손상된 파일이 있어도 나머지 파일 계속 처리

### 제한사항

⚠️ 질의응답 매칭은 시간순 규칙 기반 (AI 분석 아님)
⚠️ 첨부파일은 처리하지 않음 (본문만 추출)
⚠️ .eml 형식만 지원 (.msg, .pst 등은 미지원)

## 트러블슈팅

### 문제: "ModuleNotFoundError: No module named 'openpyxl'"

**해결책:**
```bash
pip install openpyxl
```

### 문제: GUI가 실행되지 않음

**해결책:**
- tkinter가 설치되어 있는지 확인
- Windows: Python 설치 시 기본 포함
- Linux: `sudo apt-get install python3-tk`
- macOS: Python 설치 시 기본 포함

### 문제: 한글이 깨짐

**해결책:**
- 이메일 파일이 UTF-8 또는 지원되는 인코딩인지 확인
- 코드는 자동으로 인코딩 오류를 무시하도록 설정됨 (`errors='ignore'`)

### 문제: 엑셀 파일이 열리지 않음

**해결책:**
- openpyxl 버전 확인: `pip install --upgrade openpyxl`
- 엑셀 파일이 다른 프로그램에서 열려있는지 확인

## 라이선스

이 프로젝트는 자유롭게 사용 가능합니다.

## 기여

버그 리포트나 기능 제안은 언제든지 환영합니다!

## 업데이트 이력

- **v1.0** (2025): 초기 버전 릴리스
  - .eml 파일 파싱
  - GUI 및 CLI 인터페이스
  - 질의응답 추출
  - 엑셀 내보내기

## 문의

프로그램 사용 중 문제가 있거나 질문이 있으시면 이슈를 등록해주세요.

---

**Happy Email Parsing! 📧✨**
